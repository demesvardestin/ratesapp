<div class="row">
    <% @promos.sorted.each do |promo| %>
        <div class="modal fade" id="viewPromoDetails<%= promo.id %>" tabindex="-1" role="dialog"
            aria-labelledby="exampleModalLabel" aria-hidden="true">
          <div class="modal-dialog" role="document">
            <div class="modal-content box-shadow">
              <div class="modal-header" style="border-bottom: none;">
                <h5 class="modal-title" id="exampleModalLabel">What you get for this rate</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
              <div class="modal-body" style="text-align: left !important;">
                <%= promo.package_details %>
              </div>
            </div>
          </div>
        </div>
        
        <div class="col-md-3" align="center">
            <div class="card package-card">
                <div class="card-header basic-package"
                    style="color: #fff; font-weight: bold; border-bottom: none;
                        padding-top: 25px;">
                    <i class="fas fa-award" style="font-size: 25px;"></i>
                    <h3>$<%= promo.package_price %></h3>
                    <h6 data-toggle="modal" data-target="#viewPromoDetails<%= promo.id %>"
                        style="cursor: pointer;">
                        View Details
                    </h6>
                </div>
                <div class="card-footer" style="padding: 0;">
                    <% if current_user && current_user == @promoter %>
                        <a class="btn btn-link btn-block" style="text-decoration: none;"
                            href="/promos/<%= promo.id %>/edit">
                            Edit
                        </a>
                    <% else %>
                        <%= link_to "Select",
                            new_promo_request_path(:promoter => @promoter.username, :promo_id => promo.id),
                            class:"btn btn-link btn-block", style:"color: #737374; text-decoration: none; font-weight: bold;" %>
                    <% end %>
                </div>
            </div>
        </div>
    <% end %>
</div>