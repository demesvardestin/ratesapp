<style type="text/css">
    nav {
        display: <%= 'none !important' if @promoter %>;
    }
    
    body {
        padding-top: <%= @promoter.present? ? '30px !important' : '0 !important' %>;
        background: <%= @promoter.present? ? "#{@promoter.theme_color}" : '#fff' %>;
    }
    
    #platform-logo a, .profile-link a {
        color: <%= "#fff !important;" if @promoter && @promoter.theme_color != '#eef3f5' %>;
    }
    
    #base-body {
      min-height: calc(100vh - 65px);
      margin-bottom: 15px;
    }
    
    footer {
        display: none;
    }
    
    #banner-alert {
        top: 0 !important;
    }
</style>